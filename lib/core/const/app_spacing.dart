import 'package:flutter/widgets.dart';

const double page = 20;
const double radiusMedium = 12;

const EdgeInsets pagePadding = EdgeInsets.symmetric(horizontal: page);
const EdgeInsets leftPadding = EdgeInsets.only(left: page);
const EdgeInsets rightPadding = EdgeInsets.only(right: page);
