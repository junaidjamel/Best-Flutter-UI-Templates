import 'package:flutter/material.dart';

class SubscriptionTrackerView extends StatelessWidget {
  const SubscriptionTrackerView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
